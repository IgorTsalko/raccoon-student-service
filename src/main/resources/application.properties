spring.datasource.url=jdbc:postgresql://localhost:5444/raccoon
spring.datasource.username=postgres
spring.datasource.password=12345

spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-timeout=3000

# change to update
spring.jpa.hibernate.ddl-auto=create-drop
# uncomment and use with data.sql and schema.sql
#spring.sql.init.mode=always

spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

logging.file.name=logs/newStudentDTO.log

# Remove all the fallowing for the production
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.com.raccoon.learnapp=DEBUG
